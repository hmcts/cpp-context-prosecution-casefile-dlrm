# [DD-42991] Default Hearing Time for Unallocated hearings

Jira: https://tools.hmcts.net/jira/browse/DD-42991
Epic: Schema alignment for DLRM
Sprint: DLRM Sprint 60
Story points: 5
Priority: 3-Medium
Branch: `dev/DD-42991` (based on `team/dlrm8`)

## User story
As the **Common Platform hearing-creation process**,
I want **an XHIBIT case with a missing courtroom (and, in one case, a missing hearing time) to still produce a valid Unallocated hearing with all defendants/offences listed**,
so that **incomplete listing data from XHIBIT never blocks a hearing from being created in CP**.

> ⚠️ Open question — the Jira description states the ACs but not an explicit "As a/I want/so that". The actor/benefit above is inferred from the ACs, not stated verbatim in the ticket. Still needs human confirmation (or the actual end-user-facing actor, e.g. a listing officer).

## Background
XHIBIT cases are received into Common Platform and, after passing validation, a hearing is created from the hearing details on the case. This story covers the two cases where the courtroom is absent from the incoming hearing details — with and without a hearing time also being absent — and defines how CP should default the hearing so it still lands as a valid Unallocated hearing.

## Acceptance criteria

- [x] **AC-001: Create an unallocated hearing when courtroom and time are not provided**
  - Given an XHIBIT case has been received in Common Platform (CP) and has successfully passed validation
  - And the hearing details include a courthouse and a hearing date
  - And the hearing details do not include a courtroom
  - And the hearing details do not include a hearing time
  - When the hearing is created in Common Platform
  - Then the hearing shall be created as an Unallocated hearing
  - And default the hearing time to 10am
  - And list all defendants, offences in the hearing as per the hearing details in the case received
  - **Implemented**: `isFixedHearing` in `MigratedCaseFileAggregate` widened to drop the courtroom condition. Proven red→green via `shouldDefaultHearingTimeTo10amForUnallocatedHearingWhenTimeNotProvided`, plus converter-level and IT-level coverage confirming the defaulted time reaches the `progression.initiate-court-proceedings` payload.

- [x] **AC-002: Create an unallocated hearing when courtroom is not provided but time is provided**
  - Given an XHIBIT case has been received in Common Platform (CP) and has successfully passed validation
  - And the hearing details include a courthouse, hearing date and a hearing time
  - And the hearing details do not include a courtroom
  - When the hearing is created in Common Platform
  - Then the hearing shall be created as an Unallocated hearing
  - And list all defendants, offences in the hearing as per the hearing details in the case received
  - **Implemented**: regression-guard test `shouldNotOverwriteHearingTimeForUnallocatedHearingWhenTimeProvided`, plus converter and IT coverage.

## NFR links
- NFR-001 (Accessibility): WCAG 2.1 AA — **N/A**. Confirmed this is a backend hearing-creation/data rule with no rendered UI in scope.

## Out of scope for this story
- The combination **courtroom provided + time not provided** is explicitly deferred — not implemented or tested as part of this story. This is the pre-existing behaviour (`shouldDefaultHearingTimeTo10AmOnlyForFixedHearingWithNoWarnings`), left untouched. Needs separate clarification before any change there.

## Definition of done
- [ ] Code reviewed and approved — automated review (repo's `code-reviewer`/`spec-validator`/`qa` agents) all passed; **human PR review still pending**
- [x] All ACs covered by automated tests (unit + integration) — aggregate unit tests, converter unit test, and end-to-end IT, all green
- [x] Accessibility audit — N/A, no UI in scope (see NFR-001)
- [ ] No critical or high Snyk findings introduced — not yet run; pending CI (Stage 7)
- [ ] Deployed to and verified on sandbox — not yet done
- [ ] Jira ticket updated with test evidence — not yet done

## Notes / open questions
- User-story actor/benefit is inferred, not verbatim from the ticket — still needs confirmation.
- Courtroom-provided + time-not-provided combination — **deferred**, see "Out of scope" above.
- "10am" default timezone — **resolved**: Europe/London, converted to UTC (matches the existing `toDefaultUtcTime` convention already used for the courtroom-present case).
- PR: opened from `dev/DD-42991` against `team/dlrm8` (not `main` — `team/dlrm8` is the actual integration branch for this work).
