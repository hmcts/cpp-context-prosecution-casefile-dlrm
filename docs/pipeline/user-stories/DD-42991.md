# DD-42991

Jira: https://tools.hmcts.net/jira/browse/DD-42991 (ACs live there — not duplicated here)
Branch: `dev/DD-42991` (based on `team/dlrm8`)

## Decisions made during implementation
- **Courtroom provided + time not provided** — out of scope, deferred. Ticket doesn't cover this combination; existing behaviour (`shouldDefaultHearingTimeTo10AmOnlyForFixedHearingWithNoWarnings`) left untouched.
- **10am default timezone** — Europe/London, converted to UTC. Matches the existing `toDefaultUtcTime` convention already used for the courtroom-present case.
- **Fix**: widened `isFixedHearing` in `MigratedCaseFileAggregate` to drop the courtroom condition, so the existing default-to-10am rule applies regardless of courtroom presence.

## Open questions (not blocking, not resolved)
- Whether any `cpp-ui-*` app surfaces hearing allocation/time status — if so, WCAG 2.1 AA may apply and hasn't been checked.
- AC1/AC2 don't state an explicit actor/benefit — not confirmed with a human.

## Test coverage
Aggregate unit tests, converter unit test, end-to-end IT — all green. Not yet run: Snyk/CI, sandbox deploy.
